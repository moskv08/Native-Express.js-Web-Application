// Unit Tests for the Book service
const { sinon, chai } = require('../testModules');
const bookService = require('../../src/services/bookService');

// Add prototyp for should
chai.should();

describe('bookService', function () {
  describe('getAllBooks', function () {
    it('should call getAllBooks once', function () {

    });

    it('should return all books', function () {

    });
  });

  describe('getBookById', function () {

  });
});
